<div class="row">

  <div class="col-9 border-end <%= (!params[:start_date_range] || params[:start_date_range].empty?) ? "" : "d-none" %>">
    <%= render 'shared/fixed_date_form' %>
  </div>
  <div class="col-3 <%= (!params[:start_date_range] || params[:start_date_range].empty?) ? "" : "d-none" %>">
    <div class="d-flex justify-content-between h-100">
      <i class="fas fa-angle-double-left"></i>
      <div class = "d-flex flex-column align-items-center justify-content-center"><em style = "font-size: 0.9vw;">I'm flexible!</em></div>
    </div>
  </div>
  <div class="col-3 <%= (!params[:start_date_range] || params[:start_date_range].empty?) ? "d-none" : "" %>">
    <div class="d-flex justify-content-between">
      <div><em style = "font-size: 0.9vw;">Fixed Dates</em></div>
      <i class="fas fa-angle-double-right"></i>
    </div>
  </div>
  <div class="col-9 <%= (!params[:start_date_range] || params[:start_date_range].empty?) ? "d-none" : "" %>" >
    <%= render 'shared/date_range_form' %>
 </div>
</div>
